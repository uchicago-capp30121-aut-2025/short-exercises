"""
Short Exericses #2
"""


def eisenstein_triple(a, b, c):
    """
    Determine whether a, b, c form an Eisenstein triple of the form 
    a^2 - ab + b^2 = c^2. 
    
    Inputs:
        a (int): the first number
        b (int): the second number
        c (int): the third number

    Returns (boolean): True if a, b, and c form an Eisenstein triple, 
       False otherwise.
    """

    ### EXERCISE 1 -- Replace pass with your code
    pass


def first_negative(lst):
    """
    Determine the first negative value in the list. 

    Args:
        lst (list of ints): the list

    Returns (int): The first negative, or zero.
    """

    ### EXERCISE 2 -- Replace pass with your code
    pass


def clip_in_range(lst, lb, ub):
    """
    Modify the input list so that all of the values are between lb and ub,
    inclusive.

    Args:
        lst (list of floats): the list
        lb (float): the lower bound
        ub (float): the upper bound

    Returns (None): Nothing, modifies lst in place.
    """

    ### EXERCISE 3 -- Replace pass with your code
    pass


def expand(a, b):
    """
    Create a list from a to b, inclusive. 

    Args:
        a (int): the first integer
        b (int): the second integer

    Returns (list of ints): The list from a to b
    """
    
    ### EXERCISE 4 -- Replace pass with your code
    pass 


def same_length(lst_of_lsts): 
    """
    Determine whether every inner list has the same length.

    Args:
        list_of_lists (list of lists of ints): the list

    Retuns (boolean): True if every inner list has the same length,
        False otherwise. 
    """

    ### EXERCISE 5 -- Replace pass with your code
    pass